import React from 'react';
import styles from './SearchBar.module.css';

export default function SearchBar({ characterName }) {
  return (
    <>
      <fieldset>
        <legend>Search</legend>
        <input type='text' name='' id='' />
      </fieldset>
    </>
  );
}
